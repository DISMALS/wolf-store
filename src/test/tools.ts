function handleStr(value: string) {
    return value.split('/');
}
// export function handleStr(value: string) {
//     return value.split('/');
// }

export { handleStr };

// export handleStr;

// export default handleStr;